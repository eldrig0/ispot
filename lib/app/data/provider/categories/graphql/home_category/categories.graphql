query CategoryList($first: Int!, $after: String) {
  categories(first: $first, after: $after) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        id
        name
        backgroundImage {
          url
        }
      }
    }
  }
}
