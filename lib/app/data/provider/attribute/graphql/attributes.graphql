query attributes($categoryId: ID!) {
  attributes(filter: { inCategory: $categoryId }, first: 10) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        slug
        values {
          id
          name
          slug
        }
      }
    }
  }
}
